<%= @topics.each do |t| %>
  <h2><%= link_to t.title, topic_path(t.id) %></h2>
  <p><%= t.description %></p>

  <% if policy(t).update? %>
    <%= link_to "edit", edit_topic_path(t.id) %>
  <% end %>

<% end %>